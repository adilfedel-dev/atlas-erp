<Window x:Class="AtlasERP.Presentation.WPF.Views.SignatureCaptureView"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Capture signature"
        Width="480" Height="440"
        WindowStartupLocation="CenterOwner"
        ResizeMode="NoResize"
        Background="{DynamicResource Brush.Background.Window}">
    <Grid Margin="24">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>

        <TextBlock Grid.Row="0" Text="{Binding RoleTitle}" Style="{StaticResource Text.Heading2}" Margin="0,0,0,16" />

        <StackPanel Grid.Row="1" Margin="0,0,0,8">
            <TextBlock Text="Full name" Style="{StaticResource Text.Label}" />
            <TextBox Text="{Binding ApproverName, UpdateSourceTrigger=PropertyChanged}" />
        </StackPanel>

        <StackPanel Grid.Row="2" Margin="0,8,0,12">
            <TextBlock Text="Sign below" Style="{StaticResource Text.Label}" />
            <Border BorderBrush="{DynamicResource Brush.Border}" BorderThickness="1" CornerRadius="8" Background="White">
                <InkCanvas x:Name="SignatureInkCanvas" Background="White" Height="160" />
            </Border>
        </StackPanel>

        <TextBlock Grid.Row="3"
                   Text="{Binding ErrorMessage}"
                   Foreground="{DynamicResource Brush.Error}"
                   TextWrapping="Wrap"
                   Margin="0,0,0,8"
                   Visibility="{Binding ErrorMessage, Converter={StaticResource NullToCollapsedConverter}}" />

        <StackPanel Grid.Row="4" Orientation="Horizontal" HorizontalAlignment="Right">
            <Button Content="Clear" Click="ClearButton_Click" Margin="0,0,8,0" />
            <Button Content="Cancel" Click="CancelButton_Click" Margin="0,0,8,0" />
            <Button Content="Save signature" Style="{StaticResource Button.Primary}" Click="SaveButton_Click" />
        </StackPanel>
    </Grid>
</Window>
