<Window x:Class="AtlasERP.Presentation.WPF.Views.GenerateRunView"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Generate payroll run"
        Width="360" Height="320"
        WindowStartupLocation="CenterOwner"
        ResizeMode="NoResize"
        Background="{DynamicResource Brush.Background.Window}">
    <Grid Margin="24">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>

        <TextBlock Grid.Row="0" Text="Generate payroll run" Style="{StaticResource Text.Heading2}" Margin="0,0,0,16" />

        <StackPanel Grid.Row="1">
            <TextBlock Text="Year" Style="{StaticResource Text.Label}" />
            <TextBox Text="{Binding Year, UpdateSourceTrigger=PropertyChanged}" Margin="0,0,0,12" />

            <TextBlock Text="Month" Style="{StaticResource Text.Label}" />
            <ComboBox ItemsSource="{Binding MonthOptions}" SelectedItem="{Binding Month}" Margin="0,0,0,12" />

            <TextBlock Text="{Binding ErrorMessage}"
                       Foreground="{DynamicResource Brush.Error}"
                       TextWrapping="Wrap"
                       Visibility="{Binding ErrorMessage, Converter={StaticResource NullToCollapsedConverter}}" />
        </StackPanel>

        <StackPanel Grid.Row="2" Orientation="Horizontal" HorizontalAlignment="Right">
            <Button Content="Cancel" Command="{Binding CancelCommand}" Margin="0,0,8,0" />
            <Button Content="Generate" Style="{StaticResource Button.Primary}" Command="{Binding ConfirmCommand}" IsDefault="True" />
        </StackPanel>
    </Grid>
</Window>
