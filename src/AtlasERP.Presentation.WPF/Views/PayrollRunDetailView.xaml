<Window x:Class="AtlasERP.Presentation.WPF.Views.PayrollRunDetailView"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Payroll run"
        Width="900" Height="640"
        WindowStartupLocation="CenterOwner"
        Background="{DynamicResource Brush.Background.Window}">
    <Grid Margin="24">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>

        <Grid Grid.Row="0" Margin="0,0,0,12">
            <StackPanel>
                <TextBlock Text="{Binding PeriodLabel}" Style="{StaticResource Text.Heading1}" />
                <TextBlock Text="{Binding Status}" Style="{StaticResource Text.Subtitle}" />
            </StackPanel>
            <StackPanel Orientation="Horizontal" HorizontalAlignment="Right" VerticalAlignment="Center">
                <Button Content="Print payslip" Command="{Binding PrintSelectedPayslipCommand}" Margin="0,0,8,0" />
                <Button Content="Print all payslips" Command="{Binding PrintAllPayslipsCommand}" Margin="0,0,8,0" />
                <Button Content="Finalize run"
                        Style="{StaticResource Button.Primary}"
                        Command="{Binding FinalizeRunCommand}"
                        Visibility="{Binding IsDraft, Converter={StaticResource BoolToVisibleConverter}}" />
            </StackPanel>
        </Grid>

        <TextBlock Grid.Row="1"
                   Text="{Binding ErrorMessage}"
                   Foreground="{DynamicResource Brush.Error}"
                   Margin="0,0,0,8"
                   Visibility="{Binding ErrorMessage, Converter={StaticResource NullToCollapsedConverter}}" />

        <Grid Grid.Row="2">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="1.4*" />
                <ColumnDefinition Width="16" />
                <ColumnDefinition Width="*" />
            </Grid.ColumnDefinitions>

            <Border Grid.Column="0" Style="{StaticResource Panel.Card}" Padding="0">
                <DataGrid ItemsSource="{Binding Payslips}"
                          SelectedItem="{Binding SelectedPayslip}"
                          AutoGenerateColumns="False"
                          IsReadOnly="True"
                          CanUserAddRows="False"
                          SelectionMode="Single"
                          GridLinesVisibility="Horizontal"
                          BorderThickness="0">
                    <DataGrid.Columns>
                        <DataGridTextColumn Header="Employee" Width="*">
                            <DataGridTextColumn.Binding>
                                <MultiBinding StringFormat="{}{0} {1}">
                                    <Binding Path="Employee.FirstName" />
                                    <Binding Path="Employee.LastName" />
                                </MultiBinding>
                            </DataGridTextColumn.Binding>
                        </DataGridTextColumn>
                        <DataGridTextColumn Header="Gross" Binding="{Binding GrossPay, StringFormat=C}" Width="100" />
                        <DataGridTextColumn Header="Bonuses" Binding="{Binding TotalBonuses, StringFormat=C}" Width="100" />
                        <DataGridTextColumn Header="Deductions" Binding="{Binding TotalDeductions, StringFormat=C}" Width="100" />
                        <DataGridTextColumn Header="Net" Binding="{Binding NetPay, StringFormat=C}" Width="100" />
                    </DataGrid.Columns>
                </DataGrid>
            </Border>

            <StackPanel Grid.Column="2">
                <TextBlock Text="Line items" Style="{StaticResource Text.Heading2}" Margin="0,0,0,8" />

                <Border Style="{StaticResource Panel.Card}" Padding="0" Height="220" Margin="0,0,0,12">
                    <DataGrid ItemsSource="{Binding SelectedPayslipLineItems}"
                              SelectedItem="{Binding SelectedLineItem}"
                              AutoGenerateColumns="False"
                              IsReadOnly="True"
                              CanUserAddRows="False"
                              SelectionMode="Single"
                              GridLinesVisibility="Horizontal"
                              BorderThickness="0">
                        <DataGrid.Columns>
                            <DataGridTextColumn Header="Description" Binding="{Binding Description}" Width="*" />
                            <DataGridTextColumn Header="Type" Binding="{Binding Type}" Width="80" />
                            <DataGridTextColumn Header="Amount" Binding="{Binding Amount, StringFormat=C}" Width="80" />
                        </DataGrid.Columns>
                    </DataGrid>
                </Border>

                <Button Content="Remove selected line item" Command="{Binding RemoveLineItemCommand}" Margin="0,0,0,16"
                        Visibility="{Binding IsDraft, Converter={StaticResource BoolToVisibleConverter}}" />

                <StackPanel Visibility="{Binding IsDraft, Converter={StaticResource BoolToVisibleConverter}}">
                    <TextBlock Text="Add line item" Style="{StaticResource Text.Heading2}" Margin="0,0,0,8" />

                    <TextBlock Text="Type" Style="{StaticResource Text.Label}" />
                    <ComboBox ItemsSource="{Binding LineItemTypeOptions}" SelectedItem="{Binding NewLineItemType}" Margin="0,0,0,8" />

                    <TextBlock Text="Description" Style="{StaticResource Text.Label}" />
                    <TextBox Text="{Binding NewLineItemDescription, UpdateSourceTrigger=PropertyChanged}" Margin="0,0,0,8" />

                    <TextBlock Text="Amount" Style="{StaticResource Text.Label}" />
                    <TextBox Text="{Binding NewLineItemAmount, UpdateSourceTrigger=PropertyChanged}" Margin="0,0,0,12" />

                    <Button Content="Add" Style="{StaticResource Button.Primary}" Command="{Binding AddLineItemCommand}" HorizontalAlignment="Stretch" />
                </StackPanel>
            </StackPanel>
        </Grid>
    </Grid>
</Window>
