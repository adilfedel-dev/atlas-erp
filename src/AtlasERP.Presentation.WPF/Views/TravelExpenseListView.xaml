<UserControl x:Class="AtlasERP.Presentation.WPF.Views.TravelExpenseListView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">
    <Grid Margin="28">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>

        <Grid Grid.Row="0" Margin="0,0,0,20">
            <StackPanel>
                <TextBlock Text="Travel Expenses" Style="{StaticResource Text.Heading1}" />
                <TextBlock Text="Trip expense reports with owner and accountant sign-off" Style="{StaticResource Text.Subtitle}" Margin="0,2,0,0" />
            </StackPanel>
            <StackPanel Orientation="Horizontal" HorizontalAlignment="Right" VerticalAlignment="Center">
                <Button Content="Refresh" Command="{Binding LoadCommand}" Margin="0,0,8,0" />
                <Button Content="Open" Command="{Binding ViewReportCommand}" Margin="0,0,8,0" />
                <Button Content="Delete" Command="{Binding DeleteReportCommand}" Margin="0,0,8,0" />
                <Button Content="+ New report" Style="{StaticResource Button.Primary}" Command="{Binding AddReportCommand}" />
            </StackPanel>
        </Grid>

        <TextBlock Grid.Row="1"
                   Text="{Binding ErrorMessage}"
                   Foreground="{DynamicResource Brush.Error}"
                   Margin="0,0,0,8"
                   Visibility="{Binding ErrorMessage, Converter={StaticResource NullToCollapsedConverter}}" />

        <Border Grid.Row="2" Style="{StaticResource Panel.Card}" Padding="0">
            <DataGrid ItemsSource="{Binding Reports}"
                      SelectedItem="{Binding SelectedReport}"
                      AutoGenerateColumns="False"
                      IsReadOnly="True"
                      CanUserAddRows="False"
                      SelectionMode="Single"
                      GridLinesVisibility="Horizontal"
                      BorderThickness="0">
                <DataGrid.Columns>
                    <DataGridTextColumn Header="Employee" Width="180">
                        <DataGridTextColumn.Binding>
                            <MultiBinding StringFormat="{}{0} {1}">
                                <Binding Path="Employee.FirstName" />
                                <Binding Path="Employee.LastName" />
                            </MultiBinding>
                        </DataGridTextColumn.Binding>
                    </DataGridTextColumn>
                    <DataGridTextColumn Header="Destination" Binding="{Binding Destination}" Width="150" />
                    <DataGridTextColumn Header="Departure" Binding="{Binding DepartureDate, StringFormat=d}" Width="100" />
                    <DataGridTextColumn Header="Return" Binding="{Binding ReturnDate, StringFormat=d}" Width="100" />
                    <DataGridTextColumn Header="Status" Binding="{Binding Status}" Width="100" />
                    <DataGridTextColumn Header="Total" Binding="{Binding Total, StringFormat=C}" Width="*" />
                </DataGrid.Columns>
            </DataGrid>
        </Border>
    </Grid>
</UserControl>
