<Window x:Class="AtlasERP.Presentation.WPF.Views.EmployeeEditView"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="{Binding Title}"
        Width="560" Height="640"
        WindowStartupLocation="CenterOwner"
        Background="{DynamicResource Brush.Background.Window}">
    <Grid Margin="24">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>

        <TextBlock Grid.Row="0" Text="{Binding Title}" Style="{StaticResource Text.Heading1}" Margin="0,0,0,16" />

        <ScrollViewer Grid.Row="1" VerticalScrollBarVisibility="Auto">
            <StackPanel>
                <Grid>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*" />
                        <ColumnDefinition Width="16" />
                        <ColumnDefinition Width="*" />
                    </Grid.ColumnDefinitions>

                    <StackPanel Grid.Column="0">
                        <TextBlock Text="Employee code *" Style="{StaticResource Text.Label}" />
                        <TextBox Text="{Binding EmployeeCode, UpdateSourceTrigger=PropertyChanged}" Margin="0,0,0,12" />

                        <TextBlock Text="First name *" Style="{StaticResource Text.Label}" />
                        <TextBox Text="{Binding FirstName, UpdateSourceTrigger=PropertyChanged}" Margin="0,0,0,12" />

                        <TextBlock Text="Last name *" Style="{StaticResource Text.Label}" />
                        <TextBox Text="{Binding LastName, UpdateSourceTrigger=PropertyChanged}" Margin="0,0,0,12" />

                        <TextBlock Text="Gender" Style="{StaticResource Text.Label}" />
                        <ComboBox ItemsSource="{Binding GenderOptions}" SelectedItem="{Binding Gender}" Margin="0,0,0,12" />

                        <TextBlock Text="Date of birth" Style="{StaticResource Text.Label}" />
                        <DatePicker SelectedDate="{Binding DateOfBirth}" Margin="0,0,0,12" />

                        <TextBlock Text="National ID" Style="{StaticResource Text.Label}" />
                        <TextBox Text="{Binding NationalId, UpdateSourceTrigger=PropertyChanged}" Margin="0,0,0,12" />

                        <TextBlock Text="Personal email" Style="{StaticResource Text.Label}" />
                        <TextBox Text="{Binding PersonalEmail, UpdateSourceTrigger=PropertyChanged}" Margin="0,0,0,12" />
                    </StackPanel>

                    <StackPanel Grid.Column="2">
                        <TextBlock Text="Job title *" Style="{StaticResource Text.Label}" />
                        <TextBox Text="{Binding JobTitle, UpdateSourceTrigger=PropertyChanged}" Margin="0,0,0,12" />

                        <TextBlock Text="Hire date" Style="{StaticResource Text.Label}" />
                        <DatePicker SelectedDate="{Binding HireDate}" Margin="0,0,0,12" />

                        <TextBlock Text="Employment status" Style="{StaticResource Text.Label}" />
                        <ComboBox ItemsSource="{Binding EmploymentStatusOptions}" SelectedItem="{Binding EmploymentStatus}" Margin="0,0,0,12" />

                        <TextBlock Text="Base salary" Style="{StaticResource Text.Label}" />
                        <TextBox Text="{Binding BaseSalary, UpdateSourceTrigger=PropertyChanged}" Margin="0,0,0,12" />

                        <TextBlock Text="Work email" Style="{StaticResource Text.Label}" />
                        <TextBox Text="{Binding WorkEmail, UpdateSourceTrigger=PropertyChanged}" Margin="0,0,0,12" />

                        <TextBlock Text="Phone number" Style="{StaticResource Text.Label}" />
                        <TextBox Text="{Binding PhoneNumber, UpdateSourceTrigger=PropertyChanged}" Margin="0,0,0,12" />

                        <TextBlock Text="Bank name" Style="{StaticResource Text.Label}" />
                        <TextBox Text="{Binding BankName, UpdateSourceTrigger=PropertyChanged}" Margin="0,0,0,12" />
                    </StackPanel>
                </Grid>

                <TextBlock Text="Address" Style="{StaticResource Text.Label}" />
                <TextBox Text="{Binding Address, UpdateSourceTrigger=PropertyChanged}" Margin="0,0,0,12" />

                <TextBlock Text="Bank account number" Style="{StaticResource Text.Label}" />
                <TextBox Text="{Binding BankAccountNumber, UpdateSourceTrigger=PropertyChanged}" Margin="0,0,0,12" />

                <TextBlock Text="Notes" Style="{StaticResource Text.Label}" />
                <TextBox Text="{Binding Notes, UpdateSourceTrigger=PropertyChanged}"
                         AcceptsReturn="True" TextWrapping="Wrap"
                         Height="60" Margin="0,0,0,12" />
            </StackPanel>
        </ScrollViewer>

        <TextBlock Grid.Row="2"
                   Text="{Binding ErrorMessage}"
                   Foreground="{DynamicResource Brush.Error}"
                   TextWrapping="Wrap"
                   Margin="0,8,0,8"
                   Visibility="{Binding ErrorMessage, Converter={StaticResource NullToCollapsedConverter}}" />

        <StackPanel Grid.Row="3" Orientation="Horizontal" HorizontalAlignment="Right">
            <Button Content="Cancel" Command="{Binding CancelCommand}" Margin="0,0,8,0" />
            <Button Content="Save" Style="{StaticResource Button.Primary}" Command="{Binding SaveCommand}" IsDefault="True" />
        </StackPanel>
    </Grid>
</Window>
