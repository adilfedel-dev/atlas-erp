<Window x:Class="AtlasERP.Presentation.WPF.Views.LoginView"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="AtlasERP — Sign in"
        Width="440" Height="540"
        WindowStartupLocation="CenterScreen"
        WindowStyle="None"
        ResizeMode="CanMinimize"
        Background="{DynamicResource Brush.Background.Window}">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="40" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>

        <Grid Grid.Row="0" Background="Transparent" MouseLeftButtonDown="TitleBar_MouseLeftButtonDown">
            <StackPanel HorizontalAlignment="Right" Orientation="Horizontal">
                <Button Content="&#xE921;" Style="{StaticResource Button.WindowControl}" Click="Minimize_Click" />
                <Button Content="&#xE8BB;" Style="{StaticResource Button.WindowControl.Close}" Click="Close_Click" />
            </StackPanel>
        </Grid>

        <Border Grid.Row="1"
                Width="360"
                Style="{StaticResource Panel.Card}"
                Padding="36"
                HorizontalAlignment="Center"
                VerticalAlignment="Center">
            <StackPanel>
                <Border Width="44" Height="44" CornerRadius="12" Background="{DynamicResource Brush.Accent}"
                        HorizontalAlignment="Center" Margin="0,0,0,16">
                    <TextBlock Text="A" FontSize="22" FontWeight="Bold" Foreground="White"
                               HorizontalAlignment="Center" VerticalAlignment="Center" />
                </Border>

                <TextBlock Text="Welcome back" Style="{StaticResource Text.Heading1}" HorizontalAlignment="Center" />
                <TextBlock Text="Sign in to AtlasERP to continue"
                           Style="{StaticResource Text.Subtitle}"
                           Margin="0,4,0,28"
                           HorizontalAlignment="Center" />

                <TextBlock Text="Username" Style="{StaticResource Text.Label}" />
                <TextBox Text="{Binding Username, UpdateSourceTrigger=PropertyChanged}" Margin="0,0,0,16" />

                <TextBlock Text="Password" Style="{StaticResource Text.Label}" />
                <PasswordBox x:Name="PasswordInput" Margin="0,0,0,8" PasswordChanged="PasswordInput_PasswordChanged" />

                <TextBlock Text="{Binding ErrorMessage}"
                           Foreground="{DynamicResource Brush.Error}"
                           TextWrapping="Wrap"
                           Margin="0,0,0,8"
                           Visibility="{Binding ErrorMessage, Converter={StaticResource NullToCollapsedConverter}}" />

                <Button Content="Sign in"
                        Style="{StaticResource Button.Primary}"
                        Command="{Binding LoginCommand}"
                        IsDefault="True"
                        Margin="0,8,0,0"
                        HorizontalAlignment="Stretch" />

                <TextBlock Text="Signing in..."
                           Style="{StaticResource Text.Caption}"
                           Margin="0,12,0,0"
                           HorizontalAlignment="Center"
                           Visibility="{Binding IsBusy, Converter={StaticResource BoolToVisibleConverter}}" />
            </StackPanel>
        </Border>
    </Grid>
</Window>
