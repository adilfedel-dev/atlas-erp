<UserControl x:Class="AtlasERP.Presentation.WPF.Views.EmployeeListView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">
    <Grid Margin="28">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>

        <Grid Grid.Row="0" Margin="0,0,0,20">
            <StackPanel>
                <TextBlock Text="Employees" Style="{StaticResource Text.Heading1}" />
                <TextBlock Text="Manage the people on your team" Style="{StaticResource Text.Subtitle}" Margin="0,2,0,0" />
            </StackPanel>
            <StackPanel Orientation="Horizontal" HorizontalAlignment="Right" VerticalAlignment="Center">
                <Button Content="Refresh" Command="{Binding LoadCommand}" Margin="0,0,8,0" />
                <Button Content="Edit" Command="{Binding EditEmployeeCommand}" Margin="0,0,8,0" />
                <Button Content="Delete" Command="{Binding DeleteEmployeeCommand}" Margin="0,0,8,0" />
                <Button Content="+ Add employee" Style="{StaticResource Button.Primary}" Command="{Binding AddEmployeeCommand}" />
            </StackPanel>
        </Grid>

        <TextBlock Grid.Row="1"
                   Text="{Binding ErrorMessage}"
                   Foreground="{DynamicResource Brush.Error}"
                   Margin="0,0,0,8"
                   Visibility="{Binding ErrorMessage, Converter={StaticResource NullToCollapsedConverter}}" />

        <Border Grid.Row="2" Style="{StaticResource Panel.Card}" Padding="0">
            <DataGrid ItemsSource="{Binding Employees}"
                      SelectedItem="{Binding SelectedEmployee}"
                      AutoGenerateColumns="False"
                      IsReadOnly="True"
                      CanUserAddRows="False"
                      SelectionMode="Single"
                      GridLinesVisibility="Horizontal"
                      BorderThickness="0">
                <DataGrid.Columns>
                    <DataGridTextColumn Header="Code" Binding="{Binding EmployeeCode}" Width="90" />
                    <DataGridTextColumn Header="First name" Binding="{Binding FirstName}" Width="140" />
                    <DataGridTextColumn Header="Last name" Binding="{Binding LastName}" Width="140" />
                    <DataGridTextColumn Header="Job title" Binding="{Binding JobTitle}" Width="180" />
                    <DataGridTextColumn Header="Status" Binding="{Binding EmploymentStatus}" Width="100" />
                    <DataGridTextColumn Header="Hire date" Binding="{Binding HireDate, StringFormat=d}" Width="110" />
                    <DataGridTextColumn Header="Work email" Binding="{Binding WorkEmail}" Width="*" />
                </DataGrid.Columns>
            </DataGrid>
        </Border>
    </Grid>
</UserControl>
