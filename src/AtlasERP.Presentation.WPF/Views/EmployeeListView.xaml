<UserControl x:Class="AtlasERP.Presentation.WPF.Views.EmployeeListView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">
    <Grid Margin="24">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>

        <Grid Grid.Row="0" Margin="0,0,0,16">
            <TextBlock Text="Employees"
                       FontSize="22" FontWeight="Bold"
                       Foreground="{DynamicResource Brush.Text.Primary}" />
            <StackPanel Orientation="Horizontal" HorizontalAlignment="Right">
                <Button Content="Refresh" Command="{Binding LoadCommand}" Padding="10,6" Margin="0,0,8,0" />
                <Button Content="Edit" Command="{Binding EditEmployeeCommand}" Padding="10,6" Margin="0,0,8,0" />
                <Button Content="Delete" Command="{Binding DeleteEmployeeCommand}" Padding="10,6" Margin="0,0,8,0" />
                <Button Content="+ Add employee"
                        Command="{Binding AddEmployeeCommand}"
                        Padding="10,6"
                        Background="{DynamicResource Brush.Accent}"
                        Foreground="White"
                        BorderThickness="0" />
            </StackPanel>
        </Grid>

        <TextBlock Grid.Row="1"
                   Text="{Binding ErrorMessage}"
                   Foreground="{DynamicResource Brush.Error}"
                   Margin="0,0,0,8"
                   Visibility="{Binding ErrorMessage, Converter={StaticResource NullToCollapsedConverter}}" />

        <DataGrid Grid.Row="2"
                  ItemsSource="{Binding Employees}"
                  SelectedItem="{Binding SelectedEmployee}"
                  AutoGenerateColumns="False"
                  IsReadOnly="True"
                  CanUserAddRows="False"
                  SelectionMode="Single"
                  GridLinesVisibility="Horizontal"
                  Background="{DynamicResource Brush.Background.Surface}"
                  Foreground="{DynamicResource Brush.Text.Primary}"
                  BorderBrush="{DynamicResource Brush.Border}"
                  BorderThickness="1">
            <DataGrid.Columns>
                <DataGridTextColumn Header="Code" Binding="{Binding EmployeeCode}" Width="90" />
                <DataGridTextColumn Header="First name" Binding="{Binding FirstName}" Width="140" />
                <DataGridTextColumn Header="Last name" Binding="{Binding LastName}" Width="140" />
                <DataGridTextColumn Header="Job title" Binding="{Binding JobTitle}" Width="180" />
                <DataGridTextColumn Header="Status" Binding="{Binding EmploymentStatus}" Width="100" />
                <DataGridTextColumn Header="Hire date" Binding="{Binding HireDate, StringFormat=d}" Width="110" />
                <DataGridTextColumn Header="Work email" Binding="{Binding WorkEmail}" Width="*" />
            </DataGrid.Columns>
        </DataGrid>
    </Grid>
</UserControl>
