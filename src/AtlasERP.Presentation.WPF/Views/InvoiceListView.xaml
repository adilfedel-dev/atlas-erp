<UserControl x:Class="AtlasERP.Presentation.WPF.Views.InvoiceListView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">
    <Grid Margin="28">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>

        <Grid Grid.Row="0" Margin="0,0,0,20">
            <StackPanel>
                <TextBlock Text="Invoices" Style="{StaticResource Text.Heading1}" />
                <TextBlock Text="Bill customers and track payments" Style="{StaticResource Text.Subtitle}" Margin="0,2,0,0" />
            </StackPanel>
            <StackPanel Orientation="Horizontal" HorizontalAlignment="Right" VerticalAlignment="Center">
                <Button Content="Refresh" Command="{Binding LoadCommand}" Margin="0,0,8,0" />
                <Button Content="Open" Command="{Binding ViewInvoiceCommand}" Margin="0,0,8,0" />
                <Button Content="Delete" Command="{Binding DeleteInvoiceCommand}" Margin="0,0,8,0" />
                <Button Content="+ New invoice" Style="{StaticResource Button.Primary}" Command="{Binding AddInvoiceCommand}" />
            </StackPanel>
        </Grid>

        <TextBlock Grid.Row="1"
                   Text="{Binding ErrorMessage}"
                   Foreground="{DynamicResource Brush.Error}"
                   Margin="0,0,0,8"
                   Visibility="{Binding ErrorMessage, Converter={StaticResource NullToCollapsedConverter}}" />

        <Border Grid.Row="2" Style="{StaticResource Panel.Card}" Padding="0">
            <DataGrid ItemsSource="{Binding Invoices}"
                      SelectedItem="{Binding SelectedInvoice}"
                      AutoGenerateColumns="False"
                      IsReadOnly="True"
                      CanUserAddRows="False"
                      SelectionMode="Single"
                      GridLinesVisibility="Horizontal"
                      BorderThickness="0">
                <DataGrid.Columns>
                    <DataGridTextColumn Header="Number" Binding="{Binding InvoiceNumber}" Width="110" />
                    <DataGridTextColumn Header="Customer" Binding="{Binding Customer.Name}" Width="180" />
                    <DataGridTextColumn Header="Issue date" Binding="{Binding IssueDate, StringFormat=d}" Width="100" />
                    <DataGridTextColumn Header="Due date" Binding="{Binding DueDate, StringFormat=d}" Width="100" />
                    <DataGridTextColumn Header="Status" Binding="{Binding Status}" Width="90" />
                    <DataGridTextColumn Header="Total" Binding="{Binding Total, StringFormat=C}" Width="*" />
                </DataGrid.Columns>
            </DataGrid>
        </Border>
    </Grid>
</UserControl>
